akka {
  loglevel = DEBUG
  event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
}

spray.can {
    server {
      request-timeout = 4s
      server-header = ""
      remote-address-header = on
    }
}
